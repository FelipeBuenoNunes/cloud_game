/**
 * @swagger
 * components:
 *   schemas:
 *     getBalanceResponse:
 *       type: object
 *       properties:
 *         balance:
 *           type: number
 *           example: 0
 */

export interface getBalanceResponse {
    balance: number
}