/**
 * @swagger
 * components:
 *   schemas:
 *     walletSuccessResponse:
 *       type: object
 *       properties:
 *         success:
 *           type: boolean
 *           example: false
 */

export interface walletSuccessResponse {
    success: boolean
}